package com.luthfirr.tokoapp.domain.usecase

import com.luthfirr.tokoapp.data.Resource
import com.luthfirr.tokoapp.domain.repository.StoreRepository
import kotlinx.coroutines.flow.Flow
import javax.inject.Inject

class StoreInteractor
//@Inject constructor(private val storeRepository: StoreRepository) : StoreUseCase
{
//    override fun getLogin(username: String, password: String): Flow<Resource<Any>> = storeRepository.getLogin(username, password)
}